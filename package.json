{
  "private": true,
  "name": "gql-typed",
  "version": "0.0.0",
  "description": "GraphQL schema builder with full static typing in TypeScript",
  "repository": {
    "type": "git",
    "url": "https://github.com/basic-examples/gql-typed.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc && cpy README.md dist && package-json-minifier",
    "publish": "npm run build && cd dist && npm publish",
    "test": "cd test && npm start"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "cpy-cli": "^5.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "graphql": "^16"
  }
}
